# 검증 과정 전체 기록 (VALIDATION)

> [README.md](./README.md)의 상세 근거 문서.
> 이 프로젝트에서 데이터 누수 5종을 어떤 순서로 발견하고 제거했는지, 각 단계에서 실제로 어떤 수치가 나왔는지를 전부 기록한다.
> 수치를 좋게 보이도록 편집하지 않았고, 결론이 뒤집힌 지점도 그대로 남겼다.

---

## 목차

- [TL;DR](#tldr)
- [데이터 처리 이력](#데이터-처리-이력)
- [검증 전략](#검증-전략)
- [하이퍼파라미터 최적화 이력](#하이퍼파라미터-최적화-이력)
- [1~10. 단계별 실측 기록](#단계별-실측-기록)
- [걷어낸 문제 정리](#걷어낸-문제-정리)

---

## TL;DR

1. `shuffle=True`를 시간순 분할(`shuffle=False`) + `TimeSeriesSplit`으로 바꾸자 R2가 0.313 → 0.012로 떨어짐. 성능이 나빠진 게 아니라 남아있던 시간축 누수가 빠진 것.
2. 단일 홀드아웃(R2=0.0122)이 연말 시즌에 우연히 걸린 결과일 수 있어 5-Fold Walk-Forward 검증을 추가함 → 평균 R2 0.019(표준편차 0.187). 337일치라는 데이터 규모의 한계가 드러남.
3. Naive(전일매출)/이동평균(7일평균매출) 베이스라인과 비교하면 홀드아웃·Walk-Forward 평균 모두 XGBoost가 앞섬 — **단, 이 결론은 4번에서 뒤집힘.**
4. `인건비용`/`식자재비용`/`기타비용`/`테이블수`는 예측 시점엔 알 수 없는 값이라 빼야 했음(누수 세 번째 유형). 빼고 나니 홀드아웃 R2는 0.012 → 0.040으로 소폭 올랐지만, Walk-Forward 평균은 0.019 → -0.351로 떨어지고 5개 폴드 전부 이동평균에 짐. 그동안의 "베이스라인을 이긴다"는 결론은 `테이블수`가 만든 착시였음 — 7번 참고.
5. 이 하락이 데이터 문제인지 과적합인지 확인해보니 과적합이었음(Train R2 0.979, Test R2 0.040). 그리드를 얕은 트리 위주로 다시 짜고 `RandomizedSearchCV`로 재탐색하니 Walk-Forward 평균 R2가 -0.351 → -0.048, 5패 → 3승 2패로 개선됨 — 8번 참고.
6. 그런데 8번까지의 Walk-Forward 점수는 하이퍼파라미터를 고르는 데 그대로 참고한 지표라, 완전히 독립적인 검증이 아니었음(모델 선택 누수). 마지막 3개월(93일)을 튜닝 과정 전체에서 격리한 최종 홀드아웃으로 떼어놓고 나머지 244일 안에서만 다시 튜닝한 뒤 딱 한 번 평가하니, R2=0.0015로 사실상 0이지만 XGBoost가 Naive(-0.748)·이동평균(-0.059) 베이스라인을 R2·MAE 둘 다에서 근소하게 앞섰음 — 9번 참고.

---

## 데이터 처리 이력

### Data Loading Note — 날짜 부여 방식 변경

원본 엑셀은 시트 하나가 하루치 데이터고, 시트명 자체가 그 달의 일자(`'1'`, `'2'`, ... `'31'`)로 되어 있음. 예전에는 이 시트명을 안 쓰고 파일 시작일(2025-02-01)부터 시트 순서대로 날짜를 하루씩 붙이는 방식이었는데, **시트 하나만 빠지거나 잘못 읽혀도 그 뒤 날짜가 전부 조용히 밀리는 구조**였음. 날짜가 밀리면 날씨·CPI·공휴일 조인이 전부 엉뚱한 날에 붙게 됨.

12개 파일을 전부 대조해본 결과 실제로 밀린 곳은 없었지만, 재발을 막기 위해 파일명(`YYYYMM.xlsx`)과 시트명(일자)에서 날짜를 직접 계산하도록 바꿈. 중복 날짜 감지 로직도 함께 추가함.

### Data Cleaning Note

* 시계열 이동평균 계산으로 발생한 초기 구간 결측치(NaN)를 제거함 (365일 → 337일).
* **미래 정보가 과거 학습에 유입되는 것을 막기 위해 이동평균 계산 시 `shift(1)`을 적용**, 예측 시점 이전 데이터만 활용하도록 처리.
* 정기 휴무일이 매출 0으로 학습에 섞여 노이즈가 되고 있을 가능성도 점검함 — 원본 365일, 병합 후 337일 모두 `일매출`이 0인 날은 없었고 최솟값도 1,652,000원(2025-09-24, 추석 전날)이라 문제없음을 확인.

### 피처별 설계 근거 및 수정 이력

* **`휴일지수`** — 외식업 특성을 반영해 **금·토·일요일 및 법정 공휴일(대체공휴일 포함)**을 묶은 이진 변수. `날짜`(datetime64)와 공휴일 리스트(문자열)를 비교할 때 타입이 달라 매칭이 전부 실패할 뻔했는데, pandas 2.x가 암묵적으로 캐스팅해줘서 지금까지는 우연히 정상 동작하고 있었음. 다음 버전에서 사라질 동작이라 명시적으로 문자열 변환하도록 고침.

* **`7일/14일/28일평균매출`** — 주간·월간 매출 흐름(Trend) 반영용 rolling 변수. `shift(1)` 적용으로 당일 매출 제외.

* **`연말특수지수`** — 12월의 특정 요일(목·금·토) 및 연말 피크 시즌(12월 22일~31일)에 발생하는 대관·회식 특수를 반영한 이진 변수. 매장의 위치 특성(시청역 인근)을 반영해 설계함. 다만 1년치 데이터에는 12월이 한 번뿐이라, **학습에 쓰는 구간(dev set 244일)에서는 이 값이 항상 0**이 되는 구조적 한계가 있음(9번 참고). 트리 모델은 학습셋에서 한 번도 1을 보지 못한 피처로 분기를 만들 수 없으므로 현 시점에서는 사실상 작동하지 않는 피처다. 만들었는데 안 쓰인 게 아니라, **다년치 데이터가 쌓여 12월이 반복 등장해야 비로소 기능할 피처라는 걸 알고 남겨둔 것**임.

* **`육류가중평균` / `식료품가중평균`** — 한국은행 CPI 품목별 가중치를 반영해 직접 산출.
  (버그 수정) `육류가중평균`은 원래 품목 하나(육류)만 골라 가중평균 함수에 넘겼는데, 정수 인덱스를 그대로 넘기는 바람에 `.iloc[]`가 DataFrame이 아니라 단일 행 Series를 반환해 가중평균 계산이 (값×가중치)/가중치로 상쇄되어버렸음. 결과값 자체는 우연히 원본 육류 물가지수와 같았지만 계산이 의도한 대로 동작한 건 아니었어서, 인덱스를 리스트로 감싸 고침. 이 두 피처가 실제로 모델에 기여했는지는 10번에서 A/B 검증함.

### 누수 세 번째 유형 — 운영 가능성 위반

앞의 두 누수가 시간축을 넘어 미래 정보가 새는 문제였다면, 이건 종류가 다름.

`인건비용`/`식자재비용`/`기타비용`/`테이블수`는 **시간상으로는 과거(예측 대상일 당일)에 속하지만**, 실제로는 그날 영업이 끝나야 확정되는 값이라 "내일 아침 예측"이라는 목적에는 애초에 쓸 수 없음. `shuffle=False`나 `TimeSeriesSplit`으로는 절대 걸러지지 않는 종류의 누수다.

특히 `테이블수`는 그날 방문 손님 수와 사실상 같아서 **매출을 다른 이름으로 다시 넣는 것과 다름없었음**(feature importance 1위, 약 28%). 나머지 세 비용 피처는 월평균값이 그대로 반복 삽입된 것처럼 보여 변별력도 거의 없었음.

---

## 검증 전략

* **최종 홀드아웃 격리(현재 구조):** 그리드서치와 Walk-Forward를 전부 337일 전체로 돌리다 보니, 하이퍼파라미터를 그 점수를 보면서 골랐다는 사실 자체가 문제가 됨(9번 참고). 마지막 3개월(93일, 2025-10-31~2026-01-31)을 튜닝 과정 어디에도 노출시키지 않는 최종 홀드아웃으로 떼어놓고, 그리드서치·Walk-Forward는 나머지 244일(dev set)에서만 수행한 뒤 맨 마지막에 딱 한 번 평가하도록 `main.py`를 재구성함.

* **dev set 내부 분할:** 시계열 특성 보존을 위해 `shuffle=False`로 분할, dev set 244일 중 과거 80%로 학습 후 마지막 20%로 1차 검증.

* **(수정 이력) `shuffle=True` 문제:** 이전에는 `shuffle=True`를 사용했으나, 테스트 시점과 시간적으로 인접한 날짜(예: 테스트셋의 6월 15일 옆에 학습셋의 6월 14일·16일)가 학습 데이터에 섞여 들어가는 문제가 있었음. 시계열에서 인접한 날짜는 매출 패턴이 유사하기 때문에 모델이 실제로는 "정답 근처의 힌트"를 참고해 맞추는 셈이 되어, R2가 실제 예측 성능보다 낙관적으로 나옴.

* **단일 홀드아웃의 한계 발견:** `shuffle=False`로 전환한 뒤 실제 분할 결과를 확인해보니, 마지막 20%(2025-11-25 ~ 2026-01-31)에 12월 연말 시즌이 통째로 포함되어 있었음. 그 결과 `연말특수지수`가 **학습셋에서는 269개 전부 0**이었던 반면 **테스트셋에는 1인 행이 19개**나 존재해, 모델이 애초에 학습하지 못한 패턴으로 평가받는 상황이 발생함. 즉 단일 홀드아웃 점수는 "우연히 어떤 시즌이 테스트셋에 걸렸는가"에 따라 크게 출렁일 수 있음을 확인함.

* **Walk-Forward 검증 도입:** 위 한계를 보완하기 위해 `TimeSeriesSplit`(5-Fold) 기반 검증을 추가. 매 폴드마다 이전 시점까지의 데이터로 학습하고 다음 시점 구간으로 검증하는 방식을 5회 반복해, 특정 시즌 하나에 좌우되지 않는 평균적인 일반화 성능을 측정함.

* **(수정 이력) 탐색 단계 교차검증:** 하이퍼파라미터 탐색의 내부 CV도 기본 `KFold(shuffle=False)`에서 `TimeSeriesSplit`으로 교체함(당시에는 `GridSearchCV`를 쓰던 시점). `KFold(shuffle=False)`는 구간을 시간순으로 자르긴 하지만, 앞쪽 구간을 검증할 때는 오히려 뒤쪽(미래) 구간이 학습에 쓰이는 문제가 있어 탐색 단계에서도 미래 정보가 유입될 수 있었음.

* **베이스라인 상시 비교:** R2/MAE 수치만으로는 "모델이 얼마나 잘 예측했는가"를 판단하기 어렵기 때문에, 모델 없이 단순 규칙만으로 예측했을 때의 성능(**Naive**: 전일매출 그대로 사용 / **이동평균**: 7일평균매출 그대로 사용)을 홀드아웃과 각 Walk-Forward 폴드에서 함께 계산하도록 `evaluate_baselines()`, `walk_forward_evaluate()`에 내장함. XGBoost가 이 베이스라인들을 상회하는지가 "모델이 단순 규칙보다 유의미한 신호를 학습했는가"의 실질적 판단 기준이 됨.

---

## 하이퍼파라미터 최적화 이력

* `TimeSeriesSplit(n_splits=3)` 기반 교차검증으로 `n_estimators`, `learning_rate`, `max_depth`, `subsample`, `reg_lambda`, `min_child_weight`를 탐색.
* 원래는 `GridSearchCV`를 썼는데, 처음 그리드(`n_estimators` 500~1000, `max_depth` 3~7, 정규화 파라미터 없음)가 당시 학습 표본 규모(홀드아웃 269행, Walk-Forward 1폴드는 57행)에 비해 복잡한 쪽으로만 잡혀 있어서 과적합을 유도했음(8번 참고).
* 얕은 트리(`max_depth` 1~3)와 정규화 파라미터(`reg_lambda`, `min_child_weight`)를 추가하니 조합이 972개로 늘어나, 전수 탐색 대신 `RandomizedSearchCV`(`n_iter=150`, `random_state=42`)로 전환.
* **최종적으로는 이 탐색을 dev set 244일 안에서만 다시 수행했고, 그 결과가 최종 채택 파라미터임.**

---

## 단계별 실측 기록

### 1. 초기 상태 — 이동평균 누수

초기 모델은 데이터 누수(이동평균 계산 시 당일 매출 포함)로 인해 R2 Score가 **0.508**로 높게 나타났으나, 이는 미래 정보를 미리 학습한 과대평가된 성능이었음.

### 2. shift(1) 적용 후에도 남은 누수

`shift(1)`을 적용해 이동평균의 누수를 해결한 뒤에도, `train_test_split(shuffle=True)`를 사용했기 때문에 테스트 세트와 시간적으로 인접한 날짜들이 학습 세트에 함께 포함되는 또 다른 형태의 누수가 남아있었음(**R2 0.3134**).

### 3. 시간순 분할 전환

`shuffle=False` + `TimeSeriesSplit` 기반 GridSearchCV로 전환한 단일 홀드아웃(337일 전체 기준, 과거 80% 학습 → 마지막 20% 검증) 결과:

**R2 = 0.0122**, **MAE ≈ 648,090원**

### 4. Walk-Forward 5-Fold 검증 추가

단일 홀드아웃 점수가 특정 시즌(연말)에 좌우된 결과일 수 있어 추가 수행:

| Fold | 학습 크기 | 검증 크기 | XGBoost R2 | XGBoost MAE | Naive R2 | 이동평균 R2 | 폴드 승자 |
|---|---|---|---|---|---|---|---|
| 1 | 57 | 56 | -0.0092 | 662,421원 | -0.7705 | -0.2036 | **XGBoost** |
| 2 | 113 | 56 | 0.2332 | 469,449원 | -0.1639 | 0.1033 | **XGBoost** |
| 3 | 169 | 56 | 0.0131 | 582,499원 | -1.0235 | -0.2379 | **XGBoost** |
| 4 | 225 | 56 | -0.3064 | 785,258원 | -0.9961 | -0.1626 | **이동평균 베이스라인** |
| 5 | 281 | 56 | 0.1645 | 573,407원 | -0.4114 | -0.0541 | **XGBoost** |

**평균 R2 (XGBoost) = 0.0190 (표준편차 0.1865), 평균 MAE ≈ 614,607원 (표준편차 105,078원)**
**평균 R2 (Naive) = -0.6731 / 평균 R2 (이동평균) = -0.1109**

### 5. 해석 — 데이터 규모의 한계

폴드별 R2가 -0.31 ~ 0.23까지 크게 요동치는 것은 단일 홀드아웃 점수(0.0122 또는 이전의 -0.0329)가 우연이 아니라, **337일(1년 미만)이라는 데이터 규모 자체의 근본적인 한계**를 보여주는 신호임. 특정 계절/이벤트가 학습셋에 아예 없는 상태로 검증되는 폴드가 생길 수밖에 없어, 평균 R2(0.019)는 0에 가깝고 분산은 매우 큼.

현재 모델은 "설명력 있는 예측 모델"이라기보다 **평균 수준의 예측을 벗어나지 못하는 단계**이며, 데이터가 다년치로 누적되어야(특히 연말 시즌이 학습셋에 여러 번 반복 등장해야) 의미 있는 개선을 기대할 수 있음.

### 6. 베이스라인 비교 결과

* **홀드아웃 테스트셋:** XGBoost(R2=0.0122)가 Naive(R2=-0.5427, MAE 823,050원)와 이동평균(R2=-0.0501, MAE 694,425원)을 모두 상회. 특히 Naive의 R2가 음수라는 것은 "어제 매출을 오늘 매출로 그대로 예측"하는 방식이 단순 평균 예측보다도 못하다는 뜻으로, 매출의 일별 변동성이 생각보다 크다는 것을 보여줌.
* **Walk-Forward 5-Fold:** 평균 R2 기준으로도 XGBoost(0.0190)가 Naive(-0.6731), 이동평균(-0.1109)을 모두 크게 상회. 5개 폴드 중 4개(1, 2, 3, 5)에서 XGBoost가 최고 성능.
* **예외 — Fold 4:** 유일하게 Fold 4(학습 225일 시점)에서는 이동평균(R2=-0.1626)이 XGBoost(R2=-0.3064)보다 우세. 데이터가 아직 충분히 쌓이지 않았거나, 해당 시점 특유의 패턴(계절 전환기 등)이 학습셋에 부족했을 가능성. 이는 모델의 결함이라기보다 **데이터 규모가 작을 때는 복잡한 모델이 항상 단순 규칙을 이기는 것은 아니라는 것**을 실증적으로 보여주는 사례.

> ⚠️ 위 5, 6번은 `인건비용`/`식자재비용`/`기타비용`/`테이블수`가 피처에 포함된 상태에서 나온 수치임. **7번에서 이 피처들을 뺀 뒤 다시 재보니 결론이 바뀜.**

### 7. 누수 세 번째 유형 제거 후 재측정 — 결론이 뒤집힌 지점

`인건비용`, `식자재비용`, `기타비용`, `테이블수`를 제거하고 같은 절차(GridSearchCV + 홀드아웃 + 5-Fold Walk-Forward)로 다시 측정함.

| 구분 | 누수 포함 | 누수 제거 |
|---|---|---|
| 홀드아웃 R2 | 0.0122 | 0.0401 |
| 홀드아웃 MAE | 648,090원 | 690,492원 |
| Walk-Forward 평균 R2 | 0.0190 | **-0.3513** |
| Walk-Forward 폴드 승자 | 4/5 XGBoost | **0/5 XGBoost** |

홀드아웃 R2는 오히려 올랐지만, 이건 5번에서 확인했듯 특정 20% 구간 하나에 우연히 맞아떨어졌을 가능성이 있어 그대로 믿기는 어려움. 더 믿을 만한 Walk-Forward 평균은 0.019 → -0.351로 뚜렷하게 나빠졌고, **5개 폴드 전부 이동평균 베이스라인에 짐.**

즉 그동안 "XGBoost가 4/5 폴드에서 이긴다"고 봤던 근거는 **예측 시점에 알 수 없는 `테이블수`(≈ 그날 손님 수)를 몰래 넣어서 만든 결과**였던 셈. 이 하락이 데이터가 부족해서인지 다른 문제인지는 8번에서 이어서 봄.

### 8. Walk-Forward 하락 원인 확인 — 과적합

7번 결과가 나온 `best_params`(`max_depth=7, n_estimators=500, learning_rate=0.01`)로 학습셋 자체를 다시 채점해봄.

| | Train | Test |
|---|---|---|
| R2 | **0.979** | **0.040** |

학습셋은 98% 설명하면서 홀드아웃은 4%밖에 못 맞힘. 그리드 자체가 학습 표본(Walk-Forward 1폴드 57행, 피처 23개)에 비해 복잡한 쪽으로만 짜여 있던 게 원인으로 보여, `max_depth`를 7→3→2→1로 낮춰가며 Walk-Forward를 다시 돌려봄.

| 설정 | Walk-Forward 평균 R2 | 이동평균(-0.111) 대비 승수 |
|---|---|---|
| depth=7, n=500 (기존 best_params) | -0.351 | 0/5 |
| depth=3, n=100 | -0.278 | 0/5 |
| depth=2, n=50 | -0.080 | 3/5 |
| depth=2, n=100 + reg_lambda=5 | -0.074 | 3/5 |
| depth=1, n=50 (stump 앙상블) | -0.050 | 3/5 |

depth를 낮출수록 꾸준히 좋아지는 걸 확인하고, 얕은 트리(`max_depth` 1~3)·적은 트리 수(`n_estimators` 30~200)·정규화 파라미터를 넣어 그리드를 다시 짬. 조합이 972개로 늘어나 `RandomizedSearchCV`(`n_iter=150`)로 재탐색한 결과 `max_depth=2, n_estimators=50, learning_rate=0.03, subsample=0.7, reg_lambda=5, min_child_weight=1`이 뽑힘.

| 구분 | 기존 그리드 | 재설계 그리드 |
|---|---|---|
| Train R2 | 0.979 | 0.222 |
| 홀드아웃 R2 | 0.0401 | 0.0740 |
| 홀드아웃 MAE | 690,492원 | 642,227원 |
| Walk-Forward 평균 R2 | -0.3513 | -0.0476 |
| Walk-Forward 폴드 승자 | 0/5 | 3/5 |

Train R2가 0.98에서 0.22로 떨어진 건 정상화에 가까움. **이전 모델은 학습셋을 외우기만 했을 뿐 아무것도 배우지 못했던 것.** Walk-Forward 평균 R2는 여전히 음수라 "설명력 있는 예측"이라 하긴 이르지만, 5개 폴드 중 3개(3, 4, 5)에서 이동평균을 이기고 나머지 2개(1, 2)도 차이가 크지 않게 좁혀짐(폴드2는 XGB R2=0.086, MA R2=0.103).

여기서 "Walk-Forward 평균 R2가 -0.351에서 -0.048까지 좋아졌다"고 정리하고 싶어지지만, 그 전에 짚어야 할 문제가 하나 더 있음 — **이 개선 과정 자체가 검증 점수를 보고 모델을 고른 것**이라는 점. 9번에서 이어서 다룸.

> ⚠️ 따라서 위 재설계 그리드 파라미터는 최종이 아니며, 최종은 9번에서 dev set으로 다시 뽑은 값임.

### 9. 모델 선택 누수 점검 — 최종 홀드아웃 분리

5~8번에서 쓴 Walk-Forward 점수는 하이퍼파라미터(그리드 범위, 특히 depth 실험)를 정하는 데 그대로 참고한 지표였음. **시험을 미리 한 번 보고 그중 점수가 제일 잘 나온 공부법을 고른 다음, 그 점수를 "내 실력"이라고 발표하는 셈**이라, 그동안 보고한 수치에는 낙관 편향이 섞여 있다고 보는 게 맞음. `param_distributions`의 `max_depth: [1, 2, 3]` 범위도 depth 실험 결과를 보고 정한 거라 같은 문제임.

정석은 폴드마다 하이퍼파라미터를 다시 찾는 **Nested CV**지만, 337일 규모에서 폴드 안에 폴드를 또 나누면 학습 표본이 30행 이하로 쪼개져 탐색 자체가 무의미해짐. 그래서 Nested CV 대신, **마지막 3개월(93일, 2025-10-31~2026-01-31)을 튜닝 과정 전체에서 완전히 격리한 최종 홀드아웃**으로 떼어놓고, 나머지 244일(dev set) 안에서만 그리드서치와 Walk-Forward를 수행한 뒤 딱 한 번 평가하는 쪽을 택함.

dev set으로 다시 탐색한 최적 파라미터: `max_depth=3, n_estimators=30, learning_rate=0.03, subsample=0.9, reg_lambda=1, min_child_weight=10`

**dev set 내부 (튜닝에 쓰인 지표, 참고용 — 낙관 편향 있음):**

| | XGBoost | Naive | 이동평균 |
|---|---|---|---|
| 홀드아웃 R2 | -0.0631 | -0.7223 | -0.2157 |
| Walk-Forward 평균 R2 | -0.0101 | -0.6291 | -0.1161 |

**최종 홀드아웃 (마지막 3개월, 튜닝에 전혀 안 쓰인 구간, 1회 평가):**

| | XGBoost | Naive | 이동평균 |
|---|---|---|---|
| R2 | **0.0015** | -0.7479 | -0.0587 |
| MAE | **693,631원 (14.7%)** | 910,113원 (19.3%) | 736,859원 (15.6%) |

(MAE 옆 %는 최종 홀드아웃 평균 일매출 4,716,883원 대비 오차 비율. R2 하나로는 오차 크기가 안 와닿아서 같이 표기함.)

최종 홀드아웃 R2는 0.0015로 사실상 0이라 "설명력 있는 예측"이라 부르긴 어렵지만, **R2·MAE 두 지표 모두 XGBoost가 Naive·이동평균 두 베이스라인을 앞섬.** dev set 안에서 나온 수치는 하이퍼파라미터를 고르는 데 참고된 값이라 최종 성능으로 인용하지 않고, 위 최종 홀드아웃 수치를 이 프로젝트의 대표 성능으로 봄.

한 가지 짚어둘 조건: 최종 홀드아웃 93일에는 12월이 통째로 들어있고, dev set 244일에는 12월이 단 하루도 없음(`연말특수지수`가 dev set에서 항상 0인 이유이기도 함). 즉 이 R2=0.0015는 모델이 학습 과정에서 한 번도 보지 못한 계절 구간, **이 프로젝트에서 가장 불리한 조건의 시험에서 나온 점수인데도**, 튜닝에 참고했던 dev set 점수(-0.063 / -0.010)보다 오히려 높게 나왔음. 적어도 dev set 안에서 검증셋을 과하게 파고들어 튜닝한 상태는 아니었다고 볼 수 있는 근거이며, 1년치 데이터에서 마지막 3개월을 격리하면 이런 불리한 조건 자체는 어차피 피할 수 없다는 점도 함께 남겨둠.

> **(운영 원칙) 최종 홀드아웃은 1회 평가 후 소진됨.**
> 이 구간의 점수를 이미 확인했기 때문에, 이 시점 이후로 모델에 추가 튜닝을 하면 위 수치는 더 이상 독립적인 검증이 아니게 됨. 이 프로젝트의 성능 보고는 여기서 확정하고, 이후 추가 개선을 시도한다면 그때는 새로운 구간을 다시 격리해서 검증해야 함.

### 10. CPI가 실제로 쓰이고 있는지 점검 (A/B 검증)

`육류가중평균`/`식료품가중평균`은 월 단위 지표라 337일 데이터에서 유니크값이 11개뿐이고, 우상향 추세를 가짐. 트리 모델 입장에서는 사실상 "몇 월인지"를 알려주는 값이라 `월_sin`/`월_cos`와 정보가 겹침. 또한 학습 구간에 없던 범위의 CPI가 검증 구간에 등장하면 **XGBoost는 외삽을 못 하므로** 그냥 "학습 구간 최댓값과 동일"하게 취급됨. CPI가 계속 우상향하는 데다 dev set 자체가 시간순으로 계속 뒤로 밀리며 커지는 구조라, dev set의 Walk-Forward **5개 폴드 전부에서 이런 외삽이 발생**하는 것을 확인함 (매 폴드 검증 구간의 CPI 최댓값이 그 폴드 학습 구간의 최댓값을 항상 웃돎).

CPI 2개 피처를 뺀 채로 dev set Walk-Forward를 다시 돌려 비교함:

| | CPI 포함 | CPI 제외 |
|---|---|---|
| Walk-Forward 평균 R2 | -0.0101 | -0.0101 |

**소수점 단위까지 완전히 동일함.** feature importance를 보면 `식료품가중평균`은 0.000, `육류가중평균`도 0.062로 다른 피처(휴일지수 0.098, 전일매출 0.083 등)에 비해 작음 — 지금 그리드(depth 3, `n_estimators` 30, `min_child_weight` 10)가 워낙 얕고 정규화가 강해서, 트리가 CPI까지 내려가서 분기를 만들 여유가 없는 것으로 보임.

즉 CPI가 성능을 깎아먹고 있다기보다는, **거시 변수를 붙여봤지만 지금 모델 구조에서는 일별 예측에 사실상 기여하지 못하고 있다**는 게 정확한 결론임. 원인은 해상도 불일치(월 단위 vs 일 단위)와 트리 모델의 외삽 한계로 봄.

---

## 걷어낸 문제 정리

| # | 문제 | 유형 | 조치 | 확인 지점 |
|---|---|---|---|---|
| 1 | 이동평균 계산에 당일 매출 포함 | 시간축 누수 | `shift(1)` 적용 | 1~2번 |
| 2 | `shuffle=True`로 인접 날짜가 학습셋에 유입 | 시간축 누수 | `shuffle=False` + `TimeSeriesSplit` | 2~3번 |
| 3 | 예측 시점에 알 수 없는 운영 데이터 사용 | 운영 가능성 위반 | 피처 4개 제거 | 7번 |
| 4 | 표본 크기에 비해 과한 하이퍼파라미터 그리드 | 과적합 | 얕은 트리 중심으로 재설계 | 8번 |
| 5 | 검증 점수를 보고 하이퍼파라미터를 선택 | 모델 선택 누수 | 최종 홀드아웃 93일 격리 | 9번 |

지금 모델은 완전히 독립된 구간에서도, 그것도 가장 불리한 계절 조건에서도 베이스라인보다 근소하게는 낫다. 다만 그 차이가 크지 않고, **337일치 데이터로는 그 이상을 기대하기 어려운 지점에 왔다**고 본다. 데이터가 다년치로 쌓이면 다시 봐야 한다.